import { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.arpith.agriconnect',
  appName: 'Agriconnect',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
